# build image 
# docker build -t dotnet-hardening:1 .

# run image with terminal
# docker run --rm -it --entrypoint sh dotnet-hardening:1

# run image with app
# docker run --rm -d -p 5000:5000 dotnet-hardening:2
# http://localhost:5000/weatherforecast


# scan image 
# docker scan dotnet-hardening:2

# kubenetes
# kubectl apply -f k8s/deployment.yaml
#k get svc dotnet-shardening-service -o jsonpath='{.status.loadBalancer.ingress[0].hostname}'
# echo "http://$(kubectl get svc dotnet-shardening-service -o jsonpath='{.status.loadBalancer.ingress[0].hostname}'):5000/weatherforecast"
# echo "http://$(kubectl get svc dotnet-shardening-service -o jsonpath='{.status.loadBalancer.ingress[0].ip}'):5000/weatherforecast"

# não sei se o o comando a seguir funciona
# echo "http://$(kubectl get svc dotnet-shardening-service -o jsonpath='{.status.loadBalancer.ingress[0].ip} or {.status.loadBalancer.ingress[0].hostname}'):5000/weatherforecast"